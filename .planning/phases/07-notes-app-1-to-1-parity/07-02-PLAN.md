---
phase: 07-notes-app-1-to-1-parity
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: [src/notes.rs, styles.css]
autonomous: false
---

<objective>
Bring the Notes editor area to macOS parity with refined typography, toolbar behavior, and list/checklist interactions.

Purpose: The editor is where users live; it must feel indistinguishable from macOS Notes.
Output: Editor styling/behavior updates plus a visual verification checkpoint.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
@~/.config/opencode/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-notes-app-1-to-1-parity/07-CONTEXT.md
@.planning/phases/07-notes-app-1-to-1-parity/07-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@src/notes.rs
@styles.css
</context>

<tasks>
<task type="auto">
  <name>Task 1: Match editor typography + toolbar styling</name>
  <files>src/notes.rs, styles.css</files>
  <action>Adjust the editor toolbar visuals and editor typography to match macOS Notes (font size/weight, line-height, toolbar spacing, button affordances, and list spacing). Keep the editor as a single contenteditable region but style the first line to read as a title (larger font/weight) and body text to match Notes tone. Update checklist styling (checkbox size/spacing) and list indentation in CSS to feel native.</action>
  <verify>cargo build --all-targets --all-features</verify>
  <done>Editor typography, toolbar, and checklist/list styling reflect macOS Notes proportions and spacing.</done>
</task>

<task type="auto">
  <name>Task 2: Align editor interactions with Notes behavior</name>
  <files>src/notes.rs</files>
  <action>Update editor event handling to better match Notes behavior: ensure the title updates from the first line on input (not only blur), support list indent/outdent via Tab/Shift-Tab using `execCommand("indent")`/`execCommand("outdent")`, and improve checklist entry creation on Enter (insert a new checklist item instead of a plain line). Keep autosave behavior intact and avoid custom DOM parsing beyond the existing title extraction helper.</action>
  <verify>cargo build --all-targets --all-features</verify>
  <done>Typing updates titles immediately, list indentation works with Tab/Shift-Tab, and checklist Enter inserts a new checklist row.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Notes editor typography + interaction parity</what-built>
  <how-to-verify>
    1. Run: `trunk serve`
    2. Open Notes and create a new note.
    3. Verify the first line reads like a title and the body text matches Notes spacing.
    4. Use toolbar buttons for bold/italic/underline/lists/checklists.
    5. Use Tab/Shift-Tab to indent/outdent list items.
    6. Press Enter in a checklist item and confirm a new checklist row appears.
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe what looks wrong.</resume-signal>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `cargo fmt --all`
- [ ] `cargo clippy --all-targets --all-features -- -D warnings`
- [ ] `cargo build --all-targets --all-features`
- [ ] Manual verification passed
</verification>

<success_criteria>

- Editor styling and interactions feel indistinguishable from macOS Notes
- List/checklist behaviors match expected indentation and insertion
- No lint/build errors introduced
  </success_criteria>

<output>
After completion, create `.planning/phases/07-notes-app-1-to-1-parity/07-02-SUMMARY.md`
</output>
